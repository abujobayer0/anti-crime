import Layout from "@/components/layout/layout";
import React from "react";
const BookmarkedLayout = ({ children }: { children: React.ReactNode }) => {
  return <Layout>{children}</Layout>;
};

export default BookmarkedLayout;
